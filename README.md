# PII Extractor — Git Collaboration Exercise

## Overview

This repository contains two raw data files from which you will extract
Personally Identifiable Information (PII) using Python's built-in `re` module.
Each contributor picks **one field**, works in **one file**, and writes
**one function**. No contributor works on more than one field.

---


